# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/

# Virtual environment
.venv/
venv/

# Local config may contain a POESESSID secret — never commit it
config.json

# Local handoff/progress notes — keep out of the repo
PROGRESS.md
